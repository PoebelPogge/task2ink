# Task 2 Ink

## What you need?

## How to run?

First step is to create a `config.json` looking like in the project root directory.
```json
{
  "refreshInterval": 300,
  "calendars": [
    {
      "url": "https://caldav.server.url/calendar.ics",
      "username": "username",
      "password": "password"
    }
  ]
}
```